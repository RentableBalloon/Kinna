# Kinna Mobile App

React Native mobile application for Kinna built with Expo.

## Development

```bash
npm install
npm start
```

Then press:
- `a` to run on Android
- `i` to run on iOS
- `w` to run on web

## Build

```bash
npm run build
```
